#write a prgram to find out display area of triangle using given base and height
base=0
height=0

base=input("Enter base=")
height=input("Enter height=")

base=float(base)
height=float(height)

area=0.5*base*height
print("Area of triangle is=",area)